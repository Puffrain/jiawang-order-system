import { NextResponse } from 'next/server';
import { requireApiRole } from '@/lib/auth';
import db from '@/lib/db';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireApiRole('owner');if(auth.response)return auth.response;const body=await request.json().catch(()=>({}));const method=String(body.method||'onsite');if(!['onsite','wechat','alipay'].includes(method))return NextResponse.json({error:'支付方式无效'},{status:400});if(method!=='onsite'&&!process.env.WECHAT_PAY_MERCHANT_ID&&!process.env.ALIPAY_APP_ID)return NextResponse.json({error:'在线支付尚未配置，当前只能登记现场收款'},{status:409});const id=(await params).id;const result=db.prepare("UPDATE orders SET payment_status='paid',payment_method=?,paid_at=CURRENT_TIMESTAMP,payment_confirmed_by=?,status=CASE WHEN status='pending_payment' THEN 'pending_shipment' ELSE status END,updated_at=CURRENT_TIMESTAMP WHERE id=? AND deleted_at IS NULL AND status IN ('pending_payment','pending_shipment')").run(method,auth.session!.userId,id);if(result.changes!==1)return NextResponse.json({error:'当前订单不能确认收款'},{status:409});return NextResponse.json({ok:true,paymentStatus:'paid',paymentMethod:method});}
