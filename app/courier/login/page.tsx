'use client';
import { useState } from 'react';
export default function CourierLogin(){const[phone,setPhone]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch('/api/auth/courier/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone,password})});const b=await r.json().catch(()=>({}));if(!r.ok)return setError(b.error||'登录失败');location.assign('/courier');};return <main className="mobile-safe-screen flex min-h-screen items-center justify-center bg-slate-950 p-4"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl bg-white p-6 shadow-xl"><h1 className="text-2xl font-bold text-slate-900">送货员登录</h1><p className="mt-2 text-sm text-slate-500">只查看分配给你的送货订单</p><input required inputMode="numeric" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="手机号" className="mt-6 w-full rounded-xl border p-3"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="密码" className="mt-3 w-full rounded-xl border p-3"/>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button className="mt-5 min-h-12 w-full rounded-xl bg-orange-500 font-semibold text-white">登录</button></form></main>}
