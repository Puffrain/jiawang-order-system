# 下一个智能体接管说明

## 先读这些文件

1. `用户项目要求.md`：用户的长期要求和不可变边界。
2. `交付说明.md`：本源码包的版本来源、包含项、排除项和验收边界。
3. `运行与部署.md`：本地开发和 Docker 部署入口。
4. `README.md` 与 `docs/RELEASE_RUNBOOK.md`：系统结构、测试和发布流程。
5. `package.json` 与 `佳旺仓库系统/package.json`：两套系统的脚本和依赖。

## 当前基线

- 成熟版固定来源：`84691fbaef7f370400b30f2a6778b29d8f767077`。
- 提交说明：`feat: paginate products and constrain chat layout`。
- 该版本包含商品分页、消息区域独立滚动、买家橙色信息卡和桌面响应式适配。
- GitHub Actions run `33256017529` 已通过订单/仓库验证、三套镜像构建和订单镜像实际启动冒烟。
- 该版本曾完成生产部署终检：健康检查通过，两套 SQLite `quick_check=ok`，五个服务运行且重启次数为 0。

## 当前工作区与暂停事项

- 原打包时工作区存在未提交的支付配置改动：`lib/db.ts`、`package.json`、`pnpm-lock.yaml`、`lib/payments/`、支付迁移和测试脚本。
- 这些改动没有进入本成熟版压缩包；若后续继续支付任务，必须先单独审查差异、补测试并重新确定候选版本。
- 不要把工作区当前状态直接当成成熟版，也不要将未提交支付改动混入本压缩包的后续复制品。
- 登录后的浏览器截图验收曾暂停；这不是本包已完成的验收项。
- 独立 reviewer/acceptance 调度接口曾因参数解析错误不可用；如继续发布或清理旧镜像，应优先补做真正独立的审查和验收。

## 已完成的交付验收

- 来源清单比对：提交文件 `475/475`，缺失 `0`，额外 `0`。
- 必要交付文件检查：`15/15`。
- 秘密扫描：`PASS`。
- 聚焦契约检查：部署配置 `PASS`、买家响应式布局 `PASS`、配送/支付契约 `PASS`。
- 解压复验：`478` 个文件，逐文件 SHA-256 失败 `0`。
- ZIP 禁入项检查：`0`。
- 原始成熟版 ZIP：`output/佳旺订单系统-成熟版-84691fb-20260830.zip`。本次包含接管资料的新 ZIP 以最终回复提供的路径和哈希为准。

## 接手后的建议顺序

1. 检查 Git 状态和当前分支，区分成熟版提交与未提交工作区改动。
2. 先运行不依赖外部服务的静态契约、秘密扫描和类型检查。
3. 如涉及业务改动，明确文件所有权，补回归测试，再进行独立 reviewer 和 acceptance。
4. 如涉及生产，先做全新临时卷预览并取得用户对具体候选提交的明确批准。
5. 部署后执行健康检查、数据库完整性、媒体数量、同步队列、服务重启次数和关键错误日志终检。
6. 任务结束更新概要和接管资料；不要删除已校验备份、生产命名卷或当前不可变镜像，除非用户另行批准。

## 禁止操作

- 不要索要、记录或复制生产密码、令牌、证书或主密钥。
- 不要把数据库、上传媒体、客户资料、备份或真实 `.env` 放入源码包。
- 不要在没有批准时连接或修改生产环境。
- 不要用旧镜像自动回滚已发生数据库迁移的环境。
- 不要把主代理自己的检查写成独立 reviewer/acceptance 结果。
