# 用户项目要求与长期约束

这份文件整理项目负责人的明确要求，供后续智能体直接执行。内容只包含工作约束和产品方向，不包含账号、密码、令牌或生产数据。

## 工作方式

- 用户希望任务闭环完成：先诊断，再修改，然后验证，并明确说明验收边界。
- 用户说“修复完完成验收”时，不能只停在分析或计划阶段。
- 每次任务结束都要留下简明概要，至少记录：用户要求、实际改动、验证证据、未完成事项、风险和下一步。
- 后续智能体接管时应先阅读本文件和 `下一个智能体接管说明.md`，不要求用户重复已经确认的决定。
- 不能把计划、推测或代理自报结果写成已完成；必须以实际文件、差异、命令输出和可复现证据为准。
- 实现者不能成为唯一 reviewer 或 acceptance；如果独立代理工具不可用，必须如实说明，不得冒充独立复核。

## 产品方向

- 当前以人工录入商品为主要生产流程。
- 优先保证商品图片、分类、规格、SKU、价格、库存、人工审核、人工发布和订单同步。
- AI 配置、能力探测和相关接口可以保留，但 AI 不是本阶段的关键验收项。
- AI 未配置或不可用不得阻断人工录入、人工审核和人工发布。
- 未经用户新的明确决定，不新增 AI 模型、提示词、自动识别或用量功能。
- 商品管理应使用分页，不要一次显示全部商品；筛选变化应回到第一页。
- 客户消息页面应保持固定高度，会话列表和消息记录独立滚动，输入栏保持可见。
- 买家“我的”和“我的积分”信息卡使用品牌橙色视觉。
- 买家首页、商品目录和商品详情需要兼顾电脑大屏与手机端；手机端既有结构和交互不可被桌面适配破坏。

## 系统边界

- 根目录是订单/客户系统；`佳旺仓库系统/` 是商品录入、库存和媒体处理权威系统。
- 两套系统通过签名内部接口同步，并由 Nginx 网关对外提供订单根路径和 `/warehouse` 路径。
- 订单系统负责客户、购物车、订单、定价、积分、聊天和销售展示。
- 仓库系统负责商品身份、SKU、库存、审核、上下架和媒体。
- 人工审核后才允许发布；仓库 AI 结果只能作为候选和证据。

## 发布与安全边界

- 每次生产发布前必须有全新临时卷预览、独立审查、验收报告和用户对具体候选版本的明确批准。
- 没有明确批准时，不连接生产、不创建生产备份、不修改容器或数据卷。
- 生产数据库、WAL/SHM、媒体、备份、环境文件、凭据和证书不得进入源码包或公开仓库。
- 生产数据卷不能在发布中删除、重建或替换。
- 主密钥使用现有 `APP_MASTER_KEY` 约定；任何变更都必须先有密文迁移方案。
- 数据库迁移后出现故障时，不得自动把旧程序连接到可能不兼容的新数据库；必须停止并等待人工恢复确认。
- 网关生产入口依赖 `127.0.0.1:8080:80`，修改 Compose 时必须保留这一契约。
- `APP_ORIGIN` 必须显式设置为实际公网 HTTPS 来源；不要使用 `https://localhost` 静默回退。
- 中国网络环境可使用可靠镜像站加速依赖下载，但不得改变锁文件、版本或候选源码。

## 当前明确暂停

- 上一个未完成的支付配置任务已按用户要求暂停。
- 本交付包不包含该任务在工作区中的未提交改动。
- 截图任务和登录后的浏览器视觉验收曾按用户要求暂停；不要把它写成已完成的视觉验收。
