# 佳旺订单系统备份与恢复验收

源码备份不包含客户资料、会话、密钥、SQLite 数据文件、WAL 文件或上传目录；生产数据必须单独加密保管，恢复演练只能在隔离环境进行。

## 在线备份

1. 确认目标路径位于受保护的备份卷，且与正在运行的数据库不是同一文件。
2. 执行 `pnpm backup:sqlite -- <数据库文件> <目标备份文件>`。脚本先对源库执行 `quick_check`，再使用 SQLite 在线备份 API生成一致快照；不要直接复制正在运行的 `.db`、`-wal`、`-shm` 文件。
3. 对备份文件记录 SHA-256、字节数、生成时间、应用版本和迁移版本，限制目录访问权限，并将副本移至独立存储。
4. 上传文件目录需另行制作一致性快照，保留原相对路径；数据库与文件快照必须使用同一个恢复点标识。

## 隔离恢复演练

1. 新建空白隔离目录，不连接生产短信、支付、仓库或公网写接口。
2. 校验备份 SHA-256 后复制为测试数据库，设置测试环境的 `DATABASE_URL` 指向该副本。
3. 对副本执行 `PRAGMA integrity_check`，结果必须为 `ok`；核对迁移版本、关键表数量、最近订单时间及上传文件抽样哈希。
4. 先备份恢复副本，再执行 `pnpm migrate` 两次；两次均应成功，`schema_migrations` 不得重复，`quick_check` 仍须为 `ok`。
5. 启动隔离服务，验收管理员登录、客户登录、商品与 SKU、购物车、订单、聊天、积分、图片和仓库同步只读查询；写操作只使用测试账号。
6. 验证失败时立即丢弃隔离副本，不得把半迁移数据库回写生产。只有完整演练通过并经人工批准，才可安排正式恢复窗口。

## 正式恢复边界

正式恢复前必须停止应用写入并保留当前生产库与上传目录的可回退快照。恢复后先执行完整性检查和只读核对，再开放写流量；任何关键表数量、迁移版本或文件哈希不一致都应回退，不得现场猜表或补表。
