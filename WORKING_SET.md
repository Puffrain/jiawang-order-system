# Working Set

## 2026-08-29 商品分页、消息滚动与橙色主题已部署

- 资源等级：标准。生产已部署 GitHub CI 通过的精确提交 `84691fbaef7f370400b30f2a6778b29d8f767077`，CI run 为 `33256017529`。
- 商品管理每页 8 件并支持上一页/下一页；管理消息会话和消息记录独立滚动；买家“我的”和积分卡改为橙色。
- 当前订单镜像 `sha256:1d527fac2b746f48a667ba9dbf0a90eabff92acab45962b0c7ac1e5dd17246ee`；仓库 Web/Worker 摘要保持 `89ccfa76...` / `2764a153...`。
- 发布前备份 `/root/jiawang-backups/20260829-233636-ui-pagination-84691fb` 已校验，不得删除；生产卷、媒体和旧镜像同样不得清理。
- 终检 `FINALIZE_PASS`：两库 quick check 正常，商品/图片/媒体计数正常，待处理、失败和死信队列均为 0；五个服务运行且重启次数为 0。
- 公网健康接口 200，买家和配送员入口 307 登录重定向；网关必须保持 `127.0.0.1:8080:80`。
- 截图任务暂停；尚未执行登录后的浏览器视觉验收。独立 reviewer/acceptance 工具仍不可用，不能把主验收视为独立验收。

## 2026-08-29 订单生命周期与配送员已部署

- 资源等级：重型。老板批准的发布 `order-lifecycle-courier-20260829-r2` 已部署，源码提交为 `51f4b15`。
- GitHub 仓库 `Puffrain/jiawang-order-system` 是公开 MIT 仓库；支付接口仅保留，微信支付和支付宝均未启用。
- 当前候选包含订单双方确认、发货前客户撤回和修改、配送员页、快递查询以及货到付款确认；人工录入商品是生产主流程。
- 发布前备份位于 `/root/jiawang-backups/20260829-132121-order-lifecycle-courier-20260829-r2`，不得删除。生产命名卷、媒体和旧镜像同样不得清理。
- 线上运行订单镜像 `89aabd59...`、仓库 Web `89ccfa76...`、仓库 Worker `2764a153...`；五个服务重启次数均为 0。
- 新鲜核验：订单 `/api/health` 与仓库 `/warehouse/api/health` 正常；网关保持 `127.0.0.1:8080:80`，供宿主 Nginx 转发。
- 发布后只读数据核对：两库 `quick_check=ok`；订单商品 23、有效商品 22、图片 50 且无缺失；仓库已发布商品 22、资源 50；同步队列无待处理或死信。
- 发布静态预检已在候选容器中通过，因为主机没有 Node；应单独改进脚本以支持 Docker Node 运行时。
- 独立 reviewer/acceptance 仍未完成，不能将主代理终检视为独立验收。下一步是只读独立复核和公网 HTTPS 浏览器抽检。
- AI 接口保留但暂停扩展；人工录入仍是关键生产流程。
- 已校验备份位于 `/root/jiawang-backups/20260828-162713-baseline-v2`，不得删除。
- 商品和媒体迁移前后计数一致；两库 quick_check、健康接口、零重启和关键日志扫描通过。
- 网关需保留生产覆盖端口 `127.0.0.1:8080:80`，宿主机 Nginx 依赖它。
- 旧镜像尚未清理；先补独立 reviewer/acceptance。
