# 佳旺订单系统成熟版交付说明

## 版本信息

- 交付日期：2026-08-30
- 源码提交：`84691fbaef7f370400b30f2a6778b29d8f767077`
- 提交说明：`feat: paginate products and constrain chat layout`
- 系统范围：根目录的订单/客户系统，以及 `佳旺仓库系统/` 中的商品与库存系统
- 技术基线：Node.js 20、pnpm 10、Next.js、React、TypeScript、SQLite、Docker Compose、Nginx

本压缩包直接从上述 Git 提交生成，不包含打包时工作目录中的未提交改动。

## 已包含

- 两套系统的应用源码、公共资源、数据库迁移、后台 Worker 和自动化测试
- Dockerfile、Compose、Nginx 网关和环境变量示例
- 中英文 README、发布手册、阶段验收报告和 MIT License
- `文件校验清单.sha256`，用于核验解压后文件是否完整

## 明确排除

- `.git` 历史、`node_modules`、`.next`、缓存、日志和临时测试输出
- SQLite 数据库、WAL/SHM、商品图片、客户上传、生产备份和运行卷
- `.env`、服务器凭据、证书、API 密钥、会话密钥和其他真实秘密
- 当前工作区中尚未完成的支付配置改动
- Codex 任务交接、工作计划和活动状态文件

## 验收依据与边界

该提交对应 GitHub Actions run `33256017529`，订单/仓库验证、三套镜像构建和订单镜像实际启动冒烟均通过。该版本曾完成生产部署终检：健康检查通过，两套 SQLite `quick_check=ok`，五个服务运行且重启次数为 0。

已知边界：登录后的浏览器截图验收当时按要求暂停；本包不包含生产数据和真实环境配置，因此解压后需要先安装依赖并配置环境，不能直接恢复生产业务数据。

## 使用顺序

1. 先阅读根目录 `README.md`。
2. 本地开发或构建按 `运行与部署.md` 操作。
3. 正式部署前阅读 `docs/RELEASE_RUNBOOK.md`，使用新的生产环境文件和独立数据卷。
4. 不要把生产数据库、上传目录或真实密钥放回源码目录。
5. 使用 SHA-256 工具核验 `文件校验清单.sha256` 后再归档或转移。
