# 人工审核流程

每个导入任务都会生成候选分组和字段证据，状态为 `review_pending`。审核员可以查看所有角度、原始背标文本、规范化值、置信度和冲突，逐字段接受或修改，也可以合并/拆分分组。

发布前必须确认商品名、活动分类和至少一个规格/包装单位。审核决策带 revision、操作者、时间和意见并写入审计。AI 重跑或人工修改会使旧批准失效。只有 `published` 数据进入默认 Excel/CSV 导出。
