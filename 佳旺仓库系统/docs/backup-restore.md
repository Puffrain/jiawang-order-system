# 备份与恢复

备份由管理员手动触发，不做自动定时任务。系统进入维护模式后，对 SQLite 做一致性在线备份，同时为原图、派生图和配置生成 SHA-256 manifest，打包为加密 `.jwbackup`。临时包下载成功后清理，应用仅保留元数据和校验值。

恢复必须先检查版本、空间、manifest、文件哈希、`integrity_check` 和外键，再自动制作当前实例留底。备份解到同卷 staging 后原子切换；任一步失败都保留原实例并回滚。恢复成功后撤销全部会话。环境变量密钥不进入备份，数据库中的备用密文恢复后仍需要相同的 `APP_MASTER_KEY`。
