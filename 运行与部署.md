# 运行与部署

## 本地开发

前置要求：Node.js 20、pnpm 10。

```powershell
pnpm install --frozen-lockfile
pnpm --dir "佳旺仓库系统" install --frozen-lockfile
pnpm run typecheck
pnpm run lint
pnpm run build
```

订单系统开发服务：

```powershell
pnpm dev
```

仓库系统开发服务：

```powershell
pnpm --dir "佳旺仓库系统" dev
```

## Docker 部署

1. 参考 `.env.example`、`production.env.template` 和 `佳旺仓库系统/.env.example` 创建受控环境文件。
2. 必须设置新的 `APP_ORIGIN`、`APP_MASTER_KEY`、`SESSION_SECRET` 和 `INTEGRATION_SHARED_SECRET`。
3. 使用 `compose.yaml` 启动订单、仓库、Worker 和网关服务。
4. 数据库与媒体必须使用独立持久卷，不得放在源码包中。
5. 切换生产前执行 `docs/RELEASE_RUNBOOK.md` 中的备份、预览、迁移和终检步骤。

环境示例文件只描述变量结构，不含可用于生产的秘密。不要沿用示例密码或开发模式配置。
